# MATLAB中文显示修复指南

## 问题描述
在MATLAB图形界面中，中文字符显示为方框，但双击后可以正常显示，鼠标移出又变回方框。

## 问题原因
1. 系统缺少中文字体
2. MATLAB字体渲染设置问题
3. 字符编码不匹配
4. 图形渲染器兼容性问题

## 解决方案

### 方案1：快速修复（推荐）
已在 `analyze_fault_records_v2.m` 和 `analyze_fault_records.m` 中集成了自动修复功能，运行时会自动设置合适的字体。

### 方案2：手动修复

#### Linux系统
1. **安装中文字体**
   ```bash
   sudo apt-get update
   sudo apt-get install fonts-noto-cjk fonts-wqy-microhei fonts-wqy-zenhei
   sudo fc-cache -fv
   ```

2. **设置环境变量**
   ```bash
   export LANG=zh_CN.UTF-8
   export LC_ALL=zh_CN.UTF-8
   ```

3. **重启MATLAB**

#### Windows系统
1. 确保系统已安装中文语言包
2. 控制面板 → 区域和语言 → 管理 → 更改系统区域设置 → 选择"中文(简体，中国)"

#### macOS系统
1. 系统偏好设置 → 语言与地区 → 添加中文
2. 确保安装了中文字体

### 方案3：MATLAB内部设置
在MATLAB命令窗口运行：
```matlab
% 设置字体
set(0, 'DefaultAxesFontName', 'Monospaced');
set(0, 'DefaultTextFontName', 'Monospaced');

% 关闭TeX解释器
set(0, 'DefaultTextInterpreter', 'none');
set(0, 'DefaultAxesTickLabelInterpreter', 'none');
set(0, 'DefaultLegendInterpreter', 'none');

% 设置渲染器
set(0, 'DefaultFigureRenderer', 'painters');
```

### 方案4：诊断工具
运行诊断脚本查找具体问题：
```matlab
diagnose_chinese_display
```

## 已实施的修复

### 1. 自动字体检测和设置
- 程序启动时自动检测可用的中文字体
- 按优先级选择最合适的字体
- 自动设置所有相关的字体属性

### 2. 关闭TeX解释器
- 避免TeX解释器干扰中文显示
- 所有文本使用'none'解释器

### 3. 环境变量设置
- Linux系统自动设置UTF-8编码
- 确保正确的字符集

### 4. 渲染器优化
- 优先使用'painters'渲染器
- 提高字体渲染质量

## 验证方法
1. 运行 `test_complete_system.m` 生成完整的分析报告
2. 检查3个图形窗口中的中文是否正常显示
3. 查看生成的文本报告中的中文是否正常

## 常见问题

### Q: 修复后仍然显示方框
A: 
1. 尝试重启MATLAB
2. 检查系统是否真正安装了中文字体
3. 尝试手动指定其他字体

### Q: 只有部分中文显示正常
A: 可能是字体不完整，建议安装完整的CJK字体包

### Q: 导出的图片中文显示不正常
A: 使用print命令时指定渲染器：
```matlab
print(gcf, 'output.png', '-dpng', '-painters');
```

## 联系支持
如果问题仍未解决，请运行 `diagnose_chinese_display.m` 并保存生成的诊断报告，以便进一步分析。